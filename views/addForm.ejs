<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>
  <body>
    <div
      class="bg-gradient-to-r from-teal-400 to-sky-400 w-full min-h-screen flex justify-center items-center shadow-lg"
    >
      <div class="max-w-xs p-5 bg-white">
        <h1
          class="text-center mb-3 text-xl bg-gradient-to-t py-2 text-slate-50 font-semibold"
        >
          Form New Employee
        </h1>
        <form action="/add" method="POST" enctype="multipart/form-data">
          <div class="mb-2">
            <input
              class="px-2 py-1 w-full border-b-2 border-b-teal-400"
              type="text"
              name="name"
              placeholder="nama"
            />
          </div>
          <div>
            <input
              class="px-2 py-1 w-full border-b-2 border-b-teal-400"
              type="email"
              name="email"
              placeholder="email"
            />
          </div>
          <div class="mb-2">
            <input
              class="py-1 w-full border-b-2 border-b-teal-400 text-slate-600"
              type="file"
              placeholder="Foto"
              name="image"
            />
          </div>
          <!-- <div class="mt-2">
            <input
              id="gaji"
              class="px-2 py-1 w-full border-b-2 border-b-teal-400"
              type="text"
              name="gaji"
              placeholder="Gaji"
              disabled
            />
          </div> -->

          <div class="mt-5 mx-auto flex justify-center">
            <input
              type="submit"
              class="bg-gradient-to-r text-slate-50 from-sky-500 px-6 py-2 to-teal-500 rounded shadow-md"
              value="Add New"
            />
          </div>
        </form>
        <!-- <script>
          const setGaji = () => {
            let jabatan = document.getElementById("jabatan").value;
            let gajiInput = document.getElementById("gaji");

            switch (jabatan) {
              case "Frontend":
                gajiInput.value = 9000000;
                break;
              case "Backend":
                gajiInput.value = 12000000;
                break;
              case "Product Manager":
                gajiInput.value = 15000000;
                break;
              default:
                gajiInput.value = 5000000;
            }
            gajiInput.removeAttribute("disabled");
          };
        </script> -->
      </div>
    </div>
  </body>
</html>
